# Copy to .env.local and fill in values

# Backend API base URL (used by server-side rewrites in next.config.ts)
BACKEND_URL=http://localhost:8000

# Public URL visible to the browser (used by Axios client)
NEXT_PUBLIC_API_URL=http://localhost:8000
